# Repository instructions

The canonical project instructions are stored in `other/AGENTS.md`.
Before working on this repository, read that file completely and follow it as if it were located here.

Canonical working paths:
- model: `src/stress_model.py`;
- config: `config/model_config.json`;
- tests: `tests/`;
- research notebook: `notebooks/03_stress_model_calibration.ipynb`;
- datasets and detailed research context: `other/`;
- generated outputs: `outputs/`.
